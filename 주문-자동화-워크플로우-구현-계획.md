# 주문 자동화 워크플로우 — 구현 계획

> 약국에서 촬영한 손글씨 주문지를 **웹에서 업로드·OCR·검수**하고, 검수 완료된 주문장을
> **로컬 앱이 읽어와 웹 크롤링으로 도매상 장바구니에 자동으로 담는** 전체 워크플로우 구현 계획.
>
> 이 문서는 기존 [docs/손글씨-주문지-OCR-기능-계획.md](docs/손글씨-주문지-OCR-기능-계획.md)를
> **크롤링 주문 자동화 + 클라우드 배포**까지 확장하는 상위 계획이다. OCR 코어 로직·결정사항은
> 그 문서를 따르고, 여기서는 "어디까지 클라우드에 두고 로컬이 무엇을 하는가"의 경계와
> 크롤링 자동화를 다룬다.

---

## 1. 목적 & 최종 워크플로우

사람이 도매상 사이트에서 약을 일일이 검색해 장바구니에 담는 수고를 자동화한다.

```
① 약국 사람이 종이 주문지를 사진 촬영
② 웹 UI에 사진 업로드 → [OCR하기] → Gemini가 약품명·포장단위·수량 3필드 추출
③ 웹 UI에서 결과 검토·수정 (Human-in-the-loop)
④ [저장] → Supabase DB에 주문장 저장 (status = 크롤링 대기)
⑤ (약국 외부) 로컬 앱이 Supabase에서 주문장 읽어옴
⑥ 로컬 앱에서 최종 검수 → [웹 크롤링으로 주문하기] 버튼
⑦ Playwright가 도매상 사이트를 조작해 약품을 수량대로 장바구니에 담음
⑧ 주문 확정(결제)은 사람이 직접 — 안전장치
```

**촬영자(약국)와 앱 구동 PC(약국 외부)가 물리적으로 떨어져 있다**는 것이 이 설계의 출발점이다.

---

## 2. 핵심 아키텍처 판단

### 대전제: 두 앱은 완전히 분리된다

이 앱은 홈 화면에서 진입하는 **두 개의 독립 앱**으로 구성된다:

| 앱 | 상태 | 데이터 |
|----|------|--------|
| **품절약 서치앱** | **건드리지 않음 (그대로 유지)** | **로컬 SQLite** (`watch_list`, `exclusion_list`, `search_sessions`, `search_results`) |
| **자동 주문 솔루션** (구 "주문지 OCR") | 이번 개발 대상 | **전적으로 Supabase** (`orders`, `order_items`, **`drug_master`**) |

- 코드로 검증 완료: 품절검색 흐름([utils/search_engine.py](utils/search_engine.py))은 `drug_master`를
  **전혀 참조하지 않는다.** `drug_master`를 쓰는 건 자동주문 쪽(`drug_matcher`·`order_reconcile`·`unit_collector`)뿐 →
  두 앱의 데이터가 깨끗하게 분리되어, `drug_master`의 Supabase 이전이 품절앱에 영향 없음.
- 따라서 **로컬 SQLite = 품절약 서치앱 전용 DB**로 남고, 자동주문 솔루션은 로컬 DB를 일절 쓰지 않는다.

### 자동주문 솔루션 = 두 개의 독립 스택 (확정)

자동주문 솔루션 자체를 **코드를 공유하지 않는 두 개의 별개 스택**으로 나눠 개발한다.
두 스택은 오직 **Supabase(데이터 계약)를 통해서만** 만난다.

| | **스택 1 — Cloud Run 웹 UI** | **스택 2 — 로컬 앱** |
|---|---|---|
| 사용자 | 약국 사람 (약국 안, 원격) | 앱 PC (약국 밖 원격) |
| 역할 | 사진 업로드 · OCR · 검토·수정 · 저장 | pending 주문 읽기 · 최종검토 · 크롤링(장바구니 담기)·규격수집 |
| 런타임 | 컨테이너, **스테이트리스, Playwright 없음** | 데스크톱, **스테이트풀, Playwright 필수** |
| 추천 기술 | **FastAPI** (`ocr_service`·`drug_matcher` 재사용, 경량 이미지) | **PyWebView** (Python 데스크톱 셸, 아래 참조) |
| 접점 | Supabase | Supabase |

- **분리 이유**: ① 런타임 제약이 정반대(스테이트리스/컨테이너 vs 데스크톱/브라우저 자동화) ② 현재 [web_server.py](web_server.py)가
  `scrapers`(Playwright)·품절앱까지 임포트하는 커플링 문제가 애초에 사라짐 ③ 독립 배포·독립 진화.
- **공유 코드 없음**: 두 스택은 서로의 코드를 import하지 않는다. 데이터 계약(§3 Supabase 스키마)만 공유.

**스택 2 로컬 앱 = PyWebView (확정)**

기존 코드베이스(FastAPI + HTML/Jinja + Playwright-Python)에 정합적인 크로스플랫폼 데스크톱 셸로 **PyWebView**를 쓴다.

- **기존 Python 코드 100% 재사용** — FastAPI 백엔드 + HTML 프론트를 그대로 두고 브라우저 대신 **네이티브 앱 창**으로 띄움.
- **크로스플랫폼** — Mac(WKWebView)·Windows(WebView2). OS 내장 웹뷰라 **Electron보다 훨씬 가벼움**(Chromium 미번들).
- **PyInstaller로 그대로 패키징** (기존 빌드 파이프라인 유지). Playwright는 자기 브라우저를 따로 띄우므로 앱 창과 무관하게 동작.
- **탈락한 대안**: Electron/Tauri(JS·Rust 우선 → 스크래퍼 8개 재작성 또는 무거운 사이드카 필요), Flet/Toga(HTML UI를 위젯으로 재작성).
- 참고: Windows는 WebView2 런타임 필요하나 Win10/11에 기본 탑재라 사실상 무시 가능.

> **`drug_master` 이전 시 주의 (로컬 쓰기 경로 존재)**: `drug_master`는 세 경로로 채워진다 —
> ① 엑셀 임포트 ② **규격수집**([utils/unit_collector.py](utils/unit_collector.py): 기준 도매상 사이트를 Playwright로
> 크롤링해 포장단위 채움 — **로컬 실행**) ③ 자유입력 약품 자동등록(source='manual'). Supabase 이전 후에도
> **규격수집 크롤링은 로컬에서 돌면서 결과를 Supabase에 써야** 한다(장바구니 `cart_status` 쓰기와 동일한
> outbound 패턴 — 로컬이 Supabase에 읽기뿐 아니라 쓰기도 하게 됨).

### 배포: Cloud Run + Docker (확정) — 개발 시 반드시 준수할 제약

자동주문 솔루션의 **클라우드 웹 UI(업로드·OCR·검수·저장)** 를 Docker 이미지로 빌드해 **Google Cloud Run**에 배포한다.
크롤링은 여기 포함되지 않는다(로컬). 개발 내내 다음 제약을 전제로 코드를 작성한다:

1. **별도 경량 진입점 필요.** 현재 [web_server.py](web_server.py)는 `scrapers`(Playwright)·품절검색까지 임포트하므로
   그대로 올리면 안 된다. Cloud Run 이미지에는 **자동주문 웹 라우트만** 담는 새 진입점(예: `cloud_server.py`)을 만들고,
   Playwright/브라우저는 이미지에서 제외 → 이미지 경량·빌드 빠름. (공유 코드 `ocr_service`·모델·스키마는 임포트해 재사용)
2. **스테이트리스 (컨테이너 파일시스템 휘발성).** 지금은 주문 원본 이미지를 로컬 `data/order_images`에 저장한다
   ([web_server.py:284,336](web_server.py#L284)). Cloud Run에선 **반드시 Supabase Storage로 저장**. 로컬 디스크 영속 금지.
3. **포트.** `$PORT` 환경변수를 바인딩(Cloud Run 기본 8080). 현재도 `os.environ["PORT"]`를 읽고 `0.0.0.0`에 바인딩하므로
   ([web_server.py:1161](web_server.py#L1161)) 호환됨 — 새 진입점도 동일 패턴 유지.
4. **설정은 env var로.** `build_config.json`·`config.json` 같은 파일 설정 대신 환경변수/Supabase에서 읽는다.
5. **시크릿은 Secret Manager.** `GEMINI_API_KEY`·Supabase 서비스 키를 이미지에 굽지 말고 Cloud Run Secret으로 주입.
6. **Gemini 호출은 Cloud Run 백엔드에서 직접.** 키를 Cloud Run Secret으로 두면 되므로 **Edge Function 불필요**
   (기존 OCR 계획 문서의 "Edge Function으로 키 이전" 결정을 이걸로 대체).
7. **HEIC 지원.** `pillow-heif`가 `libheif`를 요구 → Dockerfile에서 `apt-get install libheif`(또는 base 이미지 선택) 필요.
8. **인증.** Supabase Auth **Google 로그인(OAuth)** 사용. Cloud Run 서비스는 unauthenticated invocation 허용하되,
   앱 레벨은 Supabase 세션 + RLS로 보호. Cloud Run 배포 도메인을 Google OAuth redirect URI / Supabase 허용 origin에 등록.

### 경계선: 크롤링만 로컬, 나머지는 클라우드

| 작업 | 위치 | 이유 |
|------|:----:|------|
| 사진 업로드 UI | 클라우드 | 그냥 웹 |
| OCR (Gemini 호출) | **클라우드** | 스테이트리스 API 호출. 로컬 자원 불필요 → 어디서 돌든 동일 |
| 결과 검토·수정 UI | 클라우드 | 그냥 웹 |
| 주문장 저장 | 클라우드 | Supabase (Storage + Postgres) |
| **웹 크롤링(장바구니 담기)** | **로컬** | 도매상 사이트의 **데이터센터 IP 차단·로그인 세션** 때문에 로컬 필수 |

> **핵심**: OCR을 로컬로 끌고 오면, 공인 IP 없는 로컬 PC를 클라우드가 "깨워서" 작업시켜야 하는
> 어려운 문제(터널링/job queue)가 생긴다. OCR을 클라우드에 두면 **로컬은 Supabase로 나가는
> (outbound) 읽기 연결만** 하면 되므로 공인 IP·포트포워딩·터널이 전혀 필요 없다.

### 실행 방식: 버튼 방식 (확정)

로컬 앱에서 사람이 Supabase 주문장을 불러와 검수한 뒤 `[웹 크롤링으로 주문하기]`를 누르면
그때 크롤링을 실행한다. polling·realtime 구독 불필요 — 버튼 누를 때 한 번 fetch.
(추후 무인 자동화가 필요해지면 realtime 구독을 얹는다.)

### 다이어그램

```
[약국 사람 휴대폰/PC]
      │ 사진 업로드
      ▼
┌──────────────────────────────────────────────┐
│  클라우드                                       │
│   · 웹 UI (업로드 / 검토·수정)                  │
│   · [OCR하기] → Gemini 호출 (Edge Function)     │
│           ↕                                    │
│   Supabase: Storage(이미지) · Postgres(주문장)  │
└──────────────────────────────────────────────┘
      ▲ outbound 읽기만 (버튼 누를 때 fetch)
      │
┌──────────────────────────────────────────────┐
│  약국 외부 로컬 PC (기존 FastAPI 앱)            │
│   · Supabase에서 "크롤링 대기" 주문장 조회       │
│   · 최종 검수 → [웹 크롤링으로 주문하기]         │
│   · Playwright로 도매상 장바구니 담기            │
└──────────────────────────────────────────────┘
```

---

## 3. 데이터 계약 — Supabase 스키마

기존 로컬 SQLite의 `orders`/`order_items`/`drug_master`([db.py](db.py) 참조)를 Postgres로 옮기고,
**주문 수명주기(status)** 와 이미지 참조·RLS용 `user_id`를 더한다.
(품절앱 전용 테이블 `watch_list`·`search_results` 등은 로컬 SQLite에 그대로 둔다 — 옮기지 않음.)

```sql
-- 주문장 (날짜 + 차수 = 한 주문, 기존 UNIQUE(order_date, order_round) 유지)
create table orders (
  id           uuid primary key default gen_random_uuid(),
  user_id      uuid not null references auth.users(id),   -- RLS 격리
  order_date   date not null,
  order_round  int  not null check (order_round between 1 and 3),
  status       text not null default 'reviewing',
               -- reviewing  : 웹에서 검수 중
               -- pending    : 검수완료·저장됨 → 로컬 크롤링 대기
               -- ordered    : 로컬이 장바구니 담기 완료
  image_path   text,                                      -- Supabase Storage 경로
  created_at   timestamptz not null default now(),
  unique (user_id, order_date, order_round)
);

-- 주문 품목 (검수 후 확정값)
create table order_items (
  id            uuid primary key default gen_random_uuid(),
  order_id      uuid not null references orders(id) on delete cascade,
  drug_name     text not null,
  package_unit  text,
  quantity      text,               -- OCR 안정성 위해 문자열 유지 (기존과 동일)
  distributor   text,               -- 주문 도매상 dist_key (예: "geoweb")
  cart_status   text default 'none' -- none | added | failed  (크롤링 결과 기록)
);

-- 약품 마스터 DB (자동주문 솔루션 전용 — 로컬 SQLite에서 이전)
create table drug_master (
  id             uuid primary key default gen_random_uuid(),
  user_id        uuid not null references auth.users(id),
  name           text not null,
  insurance_code text,               -- nullable·비유니크 (기존 v2 완화 유지)
  maker          text,
  maker_norm     text,
  unit           text,               -- 규격수집 크롤링이 채우는 포장단위
  unit_manual    text,               -- 뷰어에서 사용자가 직접 추가한 규격
  source         text default 'excel', -- excel | manual
  imported_at    timestamptz,
  source_file    text
);
create index on drug_master (insurance_code);

alter table orders      enable row level security;
alter table order_items enable row level security;
alter table drug_master enable row level security;
-- 정책: 본인 user_id 행만 select/insert/update (타 약국 데이터 물리적 차단)
```

**설계 노트**
- 필드는 기존 로컬 스키마와 1:1 대응 → 로컬 코드 재사용 쉬움.
- `status` 흐름: `reviewing`(웹) → `pending`(저장) → `ordered`(크롤링 완료). 로컬은 `pending`만 조회.
- `cart_status`는 크롤링 후 품목별 성공/실패를 기록해 로컬 검수 화면에서 보여준다.
- **로컬 SQLite `orders`는 유지하지 않음(확정).** 자동주문 솔루션은 로컬 DB를 쓰지 않고 Supabase만 쓴다.
- `drug_master`도 Supabase로 이전(확정). 단 규격수집 크롤링은 로컬 실행 → 결과를 Supabase에 write-back (§2 주의 참조).

---

## 4. 구현 순서 (세 덩어리)

서로 비교적 독립적이라, **데이터 계약을 먼저 고정**하는 1번부터 시작한다.

### 덩어리 1 — Supabase 스키마 + 로컬 연동 ⭐ (먼저)
데이터 계약을 확정하고 로컬 앱이 그것을 읽어오는 최소 경로를 만든다.
- [ ] Supabase 프로젝트 생성, 위 스키마 + RLS 정책 적용
- [ ] 인증 방식 확정 (기존 계획: 이메일 매직링크)
- [ ] 로컬 앱에 Supabase 클라이언트 연동 (`.env`에 URL/anon key; outbound 읽기)
- [ ] 로컬에 "크롤링 대기 주문장 목록" 조회 API + 화면 (기존 [templates/order_history.html](templates/order_history.html) 재활용)
- [ ] 수동 시드 데이터로 로컬 조회까지 end-to-end 확인 (웹 UI 없이 SQL insert로 테스트)

### 덩어리 2 — 크롤링 "장바구니 담기"
기존 스크래퍼에 검색→담기 동작을 추가한다.
- [ ] 도매상 1곳(예: primary인 `upharmmall`)으로 파일럿: 로그인 → 검색 → 장바구니 담기
- [ ] 약품명 → 사이트 검색어 매칭 ([utils/drug_matcher.py](utils/drug_matcher.py) 확장)
- [ ] 품목별 결과(added/failed)를 `cart_status`에 기록, 로컬 UI에 표시
- [ ] `[웹 크롤링으로 주문하기]` 버튼 → 위 흐름 실행 → 완료 시 `status = ordered`
- [ ] 나머지 도매상으로 확장 (base_scraper 패턴 준수)
- [ ] **안전장치**: 장바구니까지만, 주문 확정(결제)은 사람이. 실행 전 요약 확인 단계.

### 덩어리 3 — 클라우드 업로드/OCR/검토 웹 UI
약국 사람이 쓰는 웹 프론트. (기존 OCR 계획 문서의 폰 업로드 단계와 연결)
- [ ] 사진 업로드 → Supabase Storage
- [ ] [OCR하기] → Edge Function이 Gemini 호출 (키를 서버에만 보관; [utils/ocr_service.py](utils/ocr_service.py) 로직 이전)
- [ ] 검토·수정 UI ([templates/order_ocr.html](templates/order_ocr.html) 흐름 재활용)
- [ ] [저장] → `orders`/`order_items` insert, `status = pending`
- [ ] 배포 (Cloud Run / Vercel / Supabase Hosting 중 택 — 정적+Edge면 후자가 단순)

---

## 5. "장바구니 담기" 크롤링 난이도 & 리스크

구조적으로는 어렵지 않다 — [scrapers/](scrapers/)에 8개 도매상 로그인+조회가 이미 구현돼 있고,
여기에 "담기" 동작만 추가하는 것. 다만 리스크는 조회보다 크다.

| 리스크 | 완화책 |
|--------|--------|
| 수량/약품 오매칭이 곧 금전 사고 | 장바구니까지만, **결제는 사람**. 실행 전·후 요약 확인 |
| 8개 사이트 UI 변경 시 깨짐 | 도매상별 스크래퍼 격리(기존 구조 유지), 파일럿 1곳부터 |
| OCR 결과 → 검색어 변환 정확도 | `drug_matcher` 확장, 후보 제시 + 사람 확인 |
| 데이터센터 IP 차단 | **크롤링을 로컬 유지**(본 설계의 근거) |

---

## 6. 미결정 사항

**확정된 것 (더 이상 논의 불필요)**
- ✅ 자동주문 솔루션은 로컬 DB를 쓰지 않고 **전적으로 Supabase** — 로컬 `orders` 미유지.
- ✅ `drug_master`도 **Supabase로 이전**.
- ✅ 품절약 서치앱은 **건드리지 않고** 로컬 SQLite 전용 DB로 유지.
- ✅ 클라우드 웹 UI 호스팅 = **Google Cloud Run + Docker** (§2 배포 제약 참조).
- ✅ 인증 = **Supabase Auth Google 로그인(OAuth)**. (기존 계획의 매직링크 결정 대체)
- ✅ Gemini 호출은 Cloud Run 백엔드에서 직접(Secret Manager) — Edge Function 불필요.
- ✅ 스택 2(로컬 앱) = **PyWebView** (Python 자산 재사용 + Mac·Windows 데스크톱 앱). Electron/Tauri 탈락.

**아직 미결정**
- [ ] 비용 모델 — 기존 OCR 계획 문서의 "보류" 항목 승계
- [ ] 도매상별 장바구니 담기 지원 우선순위 (primary부터?)
- [ ] `drug_master` 엑셀 임포트를 클라우드 웹 UI로 옮길지 (규격수집 크롤링은 로컬 유지 확정)

---

## 7. 다음 액션

**덩어리 1부터 시작** — Supabase 프로젝트 생성 + 스키마 적용 + 로컬 조회 연동.
데이터 계약이 고정되면 덩어리 2·3을 병렬로 진행 가능.
