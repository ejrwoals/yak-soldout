Q. 현재 우리의 앱은 지오영을 기준점으로 삼고, 지오영에서는 약품명 text를 기반으로 검색을 수행하고, 나머지 도매상들은 지오영에서 수집한 보험코드를 활용해서 검색을 수행하고 있잖아?

그런데 기준점을 꼭 지오영으로 하지 않고, 사용자가 기준점이 되는 도매상을 설정에서 변경하도록 하는 기능을 구현할 수 있겠어?

구현에 있어서 걸림돌이 되는 부분이 있는지 검토해줘

---

# 기준 도매상 변경 기능 검토

## 현재 아키텍처

`execute_search_sync()` (search_engine.py:246-294) 흐름:

1. **지오영** → 약품명(text)으로 검색 → Drug 객체에 `insurance_code` 포함
2. **보험코드 수집** → 지오영 결과에서 `{보험코드: 약품명}` dict 생성 (search_engine.py:258-262)
3. **나머지 도매상** → 수집된 보험코드로 검색

---

## 걸림돌 분석

### 1. 다른 도매상에 텍스트 검색 구현이 없음 (핵심 이슈)

모든 비지오영 스크래퍼의 `search_drug()`이 빈 리스트를 반환한다.

| 도매상 | 파일 | 현황 |
|---|---|---|
| 백제 | baekje_scraper.py:93-95 | `return []` |
| 복산 | boksan_scraper.py:64-66 | `return []` |
| 지오팜 | geopharm_scraper.py:82-84 | `return []` |
| 유팜몰 | upharmmall_scraper.py:65-67 | `return []` |
| HMP몰 | hmpmall_scraper.py:65-67 | `return []` |
| 인천약품 | incheon_scraper.py:129-131 | `return []` |
| 티제이팜 | tjpharm_scraper.py:51-53 | `return []` |

기준 도매상이 되려면 **약품명 텍스트로 검색하고, 결과에서 보험코드를 반환**할 수 있어야 한다.

**가능성이 있는 후보:**
- **백제**: API의 `keyword` 파라미터(baekje_scraper.py:101)에 텍스트 약품명을 넣어도 동작할 가능성이 있음. 응답에 `BOHUM_CD`(보험코드)도 포함됨.
- **지오팜**: `#item_name` 검색 필드(geopharm_scraper.py:121-122)에 텍스트를 넣으면 검색될 가능성이 있음.

**가능성이 낮은 후보:**
- 나머지 도매상은 실제 사이트를 확인해봐야 하지만, 보험코드 전용 검색 API만 제공할 수 있음.

### 2. 검색 엔진의 구조적 이분법

현재 두 개의 완전히 다른 검색 함수가 존재한다:

- `search_geoweb_sync()` (search_engine.py:488-642) — `drug_list: List[str]`을 받아 텍스트 검색
- `search_distributor_sync()` (search_engine.py:336-485) — `insurance_codes: Dict[str, str]`을 받아 보험코드 검색

기준 도매상을 교체 가능하게 만들려면 이 두 함수를 통합하거나, **기준 도매상용 범용 검색 함수**를 새로 만들어야 한다.

### 3. 지오영 필수 의존성이 하드코딩되어 있음

- config.py:143-145 — 지오영 자격증명이 없으면 `ValueError` 발생
- web_server.py:213-214 — API에서도 지오영 계정 필수 체크
- registry.py:16 — 주석으로 "지오영은 항상 첫 번째" 명시

### 4. 텍스트 검색 결과의 품질 차이 가능성

도매상마다 같은 약품명으로 검색해도 반환되는 결과의 정확도와 보험코드 매핑 품질이 다를 수 있다. 지오영이 기준점인 이유가 단순히 설계상의 선택이 아니라, 검색 결과의 신뢰성 때문일 수 있다.

---

## 구현 가능 여부 판단

**구현 자체는 가능하지만**, 아래 선행 작업이 필요하다:

1. **각 도매상 사이트의 텍스트 검색 지원 여부를 실제로 테스트** — 가장 큰 변수. 사이트가 텍스트 검색을 지원하지 않는 도매상은 기준점이 될 수 없다.
2. **지원되는 도매상에 `search_drug()` 텍스트 검색 로직 구현** — 보험코드를 결과에 포함시켜야 함
3. **검색 엔진 리팩토링** — `execute_search_sync()`에서 기준 도매상을 동적으로 결정하는 로직
4. **설정에 `primary_distributor` 필드 추가** — 레지스트리에 `can_be_primary: bool` 플래그 추가
5. **지오영 필수 의존성 제거** — config.py, web_server.py의 하드코딩된 지오영 체크 완화

**가장 큰 걸림돌은 1번(사이트별 텍스트 검색 지원 여부)이다.** 코드 레벨이 아니라 각 도매상 웹사이트의 기능 제약이라 코드 변경만으로는 해결할 수 없다. 실제로 각 사이트에 로그인해서 텍스트 검색이 되는지 확인해야 한다.
